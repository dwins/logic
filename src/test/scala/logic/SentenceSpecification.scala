package logic

import org.scalacheck._
import Arbitrary.arbitrary, Prop.forAll

// object SentenceSpecification extends Properties("Sentence") {
//   import Generators._
// 
//   property("blah") = forAll { (s: Sentence) => Sentence.isLiteral(s) }
// }
